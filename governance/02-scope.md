# Scope

This Working Group standardizes the in-toto Attestation Framework: a format for creating, signing, and verifying authenticated metadata (attestations) about software supply chain artifacts. In scope: the Statement and envelope layers, predicate conventions, and verification semantics. Out of scope: individual predicate type specs maintained separately, key management, and transport.

Any changes of Scope are not retroactive.
